<%= form_for [article, comment] do |f| %>
     <div>
          <%= f.label :body %>
          <%= f.text_field :body %>
     </div>

     <%= f.submit "Add Comment", class: "small button right" %>
<% end %>
