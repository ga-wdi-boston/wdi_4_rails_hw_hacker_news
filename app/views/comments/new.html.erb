<%= form_for [@article, @comment] do |f| %>
<div class="field">
  <%= f.label :body, 'Write a comment' %><br>
  <%= f.text_area :body %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
