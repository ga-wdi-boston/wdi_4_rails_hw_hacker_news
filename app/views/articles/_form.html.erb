<%= form_for article do |f| %>
     <div>
          <%= f.label :title %>
          <%= f.text_field :title %>
     </div>

     <div>
          <%= f.label :url %>
          <%= f.url_field :url %>
     </div>

     <%= f.submit "Submit Article", class: "small button right" %>
<% end %>
