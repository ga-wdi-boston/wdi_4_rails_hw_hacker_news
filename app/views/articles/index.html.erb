<h1>Articles </h1><%= link_to 'New Article', new_article_path %>

<ul>

  <% @articles.each do |article| %>
    <h4><%= link_to article.title, article.url %>
    <br>
      <small><%= link_to 'Comments', article_path(article) %>
      </small>
    </h4>
  <% end %>
</ul>
