<h1>Listing Articles </h1><small><%= link_to 'New Article', new_article_path %>

<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article.url %></li>
    <li><%= link_to 'Comment', new_article_comment_path(article) %></li>
    <br>
  <% end %>
</ul>
